# Uncomment the next line to define a global platform for your project
platform :ios, '16.0'

target 'SSHClient' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # SSH Library - Uncomment when ready to use real SSH functionality
  # pod 'NMSSH', '~> 2.3'
  
  # Alternative SSH libraries:
  # pod 'Shout' # Pure Swift SSH library
  # Or use Swift Package Manager for libssh2-based solutions

end
